import React, {createContext,useState,useContext} from 'react';\nconst FinanceContext=createContext();\nexport function FinanceProvider({children}){ const [donations,setDonations]=useState([]); const [expenses,setExpenses]=useState([]); const summary={totalDonations:0,totalExpenses:0}; return <FinanceContext.Provider value={{donations,expenses,summary,setDonations,setExpenses}}>{children}</FinanceContext.Provider>; }\nexport function useFinance(){ return useContext(FinanceContext); }
